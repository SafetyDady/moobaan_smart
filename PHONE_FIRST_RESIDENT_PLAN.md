# แผนปรับปรุง: Phone-First Resident Management

## สถานะก่อนเริ่มงาน

| รายการ | ค่า |
|--------|-----|
| **Last Safe Commit** | `ee1ddbb` — `cleanup: remove temp debug endpoints` |
| **Rollback Command** | `git reset --hard ee1ddbb && git push --force origin master` |
| **วันที่** | 21 กุมภาพันธ์ 2026 |
| **Production Status** | LIVE — app.moobaan.app |

---

## ปัญหาที่พบ

Admin สร้าง Resident จาก "context ของบ้าน" → ถ้าลูกค้ามีหลายบ้าน Admin ไม่เห็นว่ามี User อยู่แล้ว → เกิด duplicate:
- user_id=6: เบอร์ 0635162459, มี LINE, บ้าน 28/73
- user_id=17: เบอร์ 0635162459, ไม่มี LINE, บ้าน 28/72 (duplicate!)

แม้ backend จะเช็คเบอร์ซ้ำแล้ว (commit `3b08d0b`) แต่ **UX ต้องเปลี่ยนให้ Admin เห็นข้อมูลก่อนกด submit**

---

## แผนงาน (6 ขั้นตอน)

### Step 1: Backend — เพิ่ม Phone Search API
**ไฟล์**: `backend/app/api/users.py`  
**Endpoint**: `GET /api/users/residents/search?phone=0635162459`  
**Response**:
```json
{
  "found": true,
  "user": {
    "id": 6,
    "full_name": "สรรชัย สายยศ",
    "phone": "0635162459",
    "email": "sanchai@example.com",
    "line_linked": true,
    "is_active": true,
    "memberships": [
      {
        "house_id": 5,
        "house_code": "28/73",
        "role": "OWNER",
        "status": "ACTIVE"
      }
    ]
  }
}
```
ถ้าไม่พบ:
```json
{
  "found": false,
  "user": null
}
```
**ความเสี่ยง**: ต่ำ — endpoint ใหม่ ไม่แก้ของเดิม

---

### Step 2: Backend — เพิ่ม Remove House Membership API
**ไฟล์**: `backend/app/api/users.py`  
**Endpoint**: `POST /api/users/residents/{user_id}/remove-house/{house_id}`  
**Logic**:
1. หา ResidentMembership (user_id, house_id)
2. Set status = INACTIVE, deactivated_at = now
3. ลบ HouseMember (legacy) ด้วย
4. ถ้า user ไม่มี active membership เลย → set user.is_active = False

**ความเสี่ยง**: ต่ำ — endpoint ใหม่ ไม่แก้ของเดิม

---

### Step 3: Frontend — เปลี่ยน AddResident.jsx เป็น Phone-First UI
**ไฟล์**: `frontend/src/pages/admin/AddResident.jsx`  
**เปลี่ยน UX เป็น 2 ขั้นตอน**:

#### ขั้นที่ 1: ค้นหาด้วยเบอร์โทร
```
┌─────────────────────────────────────────────┐
│  เพิ่มลูกบ้าน / Add Resident               │
│                                             │
│  เบอร์โทรศัพท์ *                            │
│  ┌─────────────────────┐ ┌─────────┐        │
│  │ 063-516-2459        │ │ 🔍 ค้นหา │        │
│  └─────────────────────┘ └─────────┘        │
│                                             │
│  ✅ พบผู้ใช้ในระบบ:                          │
│  ┌─────────────────────────────────────┐    │
│  │ 👤 สรรชัย สายยศ                      │    │
│  │ 📱 LINE: เชื่อมแล้ว ✅                │    │
│  │ 🏠 บ้านปัจจุบัน:                      │    │
│  │    • 28/73 (Owner, Active)           │    │
│  └─────────────────────────────────────┘    │
│                                             │
│  เลือกบ้านที่จะเพิ่ม:                        │
│  ┌────────────────────────────┐             │
│  │ ▼ 28/72 - นาย... (active) │             │
│  └────────────────────────────┘             │
│  บทบาท:                                     │
│  ┌──────────────┐                           │
│  │ ▼ Owner      │                           │
│  └──────────────┘                           │
│                                             │
│  ┌──────────────────────────┐               │
│  │ เพิ่มเข้าบ้าน 28/72      │               │
│  └──────────────────────────┘               │
└─────────────────────────────────────────────┘
```

#### ถ้าไม่พบ user → แสดงฟอร์มสร้างใหม่:
```
┌─────────────────────────────────────────────┐
│  เบอร์โทรศัพท์ *                            │
│  ┌─────────────────────┐ ┌─────────┐        │
│  │ 099-999-9999        │ │ 🔍 ค้นหา │        │
│  └─────────────────────┘ └─────────┘        │
│                                             │
│  ⚠️ ไม่พบผู้ใช้เบอร์นี้ — จะสร้างใหม่        │
│                                             │
│  ชื่อ-นามสกุล *                              │
│  ┌─────────────────────────────────────┐    │
│  │                                     │    │
│  └─────────────────────────────────────┘    │
│  อีเมล (ไม่บังคับ)                           │
│  ┌─────────────────────────────────────┐    │
│  │                                     │    │
│  └─────────────────────────────────────┘    │
│  เลือกบ้าน *                                │
│  ┌────────────────────────────┐             │
│  │ ▼ เลือกบ้าน                │             │
│  └────────────────────────────┘             │
│  บทบาท:                                     │
│  ┌──────────────┐                           │
│  │ ▼ Owner      │                           │
│  └──────────────┘                           │
│                                             │
│  ┌──────────────────────────────┐           │
│  │ สร้างและเพิ่มเข้าบ้าน        │           │
│  └──────────────────────────────┘           │
└─────────────────────────────────────────────┘
```

**ความเสี่ยง**: ปานกลาง — เปลี่ยน UI แต่ backend เดิมรองรับ

---

### Step 4: Frontend — เพิ่มปุ่ม "ถอดจากบ้าน" ใน Members.jsx
**ไฟล์**: `frontend/src/pages/admin/Members.jsx`  
**เปลี่ยน**:
- แสดง multi-house badge (ถ้า user มีมากกว่า 1 บ้าน)
- เพิ่มปุ่ม "Remove from House" ในแถว Actions
- Confirmation modal ก่อนลบ

**ปัญหา**: `GET /api/users/residents` ปัจจุบัน return 1 row ต่อ HouseMember → user ที่มี 2 บ้านจะมี 2 แถว อยู่แล้ว ✅ ไม่ต้องแก้ backend

**ความเสี่ยง**: ต่ำ — เพิ่มปุ่มใหม่

---

### Step 5: Frontend API Client — เพิ่ม API functions
**ไฟล์**: `frontend/src/api/client.js`  
**เพิ่ม**:
```javascript
usersAPI = {
  // ... existing
  searchByPhone: (phone) => GET /api/users/residents/search?phone=xxx   // NEW
  removeFromHouse: (userId, houseId) => POST /api/users/residents/{userId}/remove-house/{houseId}  // NEW
}
```
**ความเสี่ยง**: ต่ำ — เพิ่มใหม่

---

### Step 6: Fix Production Data
**ขั้นตอน**:
1. Deploy temp admin-only endpoint
2. Transfer user_id=17's membership (house 28/72) → user_id=6
3. Transfer user_id=17's HouseMember → user_id=6
4. Deactivate user_id=17
5. ลบ temp endpoint
6. Verify: user_id=6 มี 2 บ้าน (28/73 + 28/72)

**ความเสี่ยง**: ต่ำ — admin-only, ทำ 1 ครั้ง

---

## ไฟล์ที่เปลี่ยนแปลง

| ไฟล์ | การเปลี่ยน | ความเสี่ยง |
|------|-----------|-----------|
| `backend/app/api/users.py` | เพิ่ม 2 endpoints (search + remove-house) | ต่ำ |
| `frontend/src/pages/admin/AddResident.jsx` | Rewrite เป็น Phone-First UI | ปานกลาง |
| `frontend/src/pages/admin/Members.jsx` | เพิ่มปุ่ม Remove from House | ต่ำ |
| `frontend/src/api/client.js` | เพิ่ม 2 API functions | ต่ำ |

## ไฟล์ที่ไม่เปลี่ยน (สำคัญ!)

| ไฟล์ | เหตุผล |
|------|--------|
| `backend/app/api/line_auth.py` | LINE login ไม่ต้องแก้ |
| `backend/app/api/resident_auth.py` | OTP login ไม่ต้องแก้ |
| `backend/app/api/payins.py` | Pay-in ไม่เกี่ยว |
| `backend/app/api/bank_reconciliation.py` | Matching ไม่เกี่ยว |
| `backend/app/db/models/*.py` | ไม่ต้อง migrate DB |
| `backend/app/services/resident_membership.py` | Service layer ไม่แก้ |

---

## ลำดับ Commits

| # | Commit Message | ขอบเขต |
|---|----------------|--------|
| 1 | `feat: phone search API for resident lookup` | Backend endpoint |
| 2 | `feat: remove-house membership API` | Backend endpoint |
| 3 | `feat: phone-first AddResident UI` | Frontend rewrite |
| 4 | `feat: remove-from-house in Members page` | Frontend enhance |
| 5 | `fix: merge duplicate production users` | Data fix (temp) |
| 6 | `cleanup: remove temp data fix endpoint` | Cleanup |

---

## Rollback Plan

ถ้ามีปัญหา **ทุกจุด** สามารถย้อนกลับได้:

```bash
# ย้อนกลับทั้งหมดไป commit ก่อนเริ่มงาน
git reset --hard ee1ddbb
git push --force origin master
```

Production จะ auto-deploy กลับไปสถานะเดิม ภายใน 2-3 นาที

---

## Testing Checklist

หลัง deploy ต้อง test:

- [ ] Admin: ค้นหาเบอร์ที่มีอยู่ → เห็นข้อมูล user + บ้าน
- [ ] Admin: ค้นหาเบอร์ใหม่ → เห็นฟอร์มสร้าง
- [ ] Admin: เพิ่ม user เดิมเข้าบ้านใหม่ → สำเร็จ, user มี 2 บ้าน
- [ ] Admin: สร้าง user ใหม่ + เพิ่มเข้าบ้าน → สำเร็จ
- [ ] Admin: ถอด user ออกจากบ้าน → membership INACTIVE
- [ ] Resident: LINE login → ยังทำงานปกติ
- [ ] Resident: Profile → เห็นปุ่มสลับบ้าน (ถ้ามีหลายบ้าน)
- [ ] Resident: Pay-in → สร้างได้ปกติ
- [ ] Admin: Members list → แสดงถูกต้อง
