# KNOWLEDGE LOG — moobaan_smart

> บันทึกข้อสรุป/ข้อเรียนรู้/ข้อมูลใหม่ เรียงจากใหม่ → เก่า
> รูปแบบ: วันที่ + หัวข้อ + สรุปสั้น + (ถ้ามี) เหตุผล/วิธีนำไปใช้

---

## 2026-09-09

### Owner-approved automatic allocation release deployed and verified
- Owner explicitly approved commit+push of automatic allocation and invoice-read access candidate.43a52b734e0e762c2a26311c39abec12ac6dc62c matchesapprovedtreeebca0b71867f61648d739a0b49408b28e9f00050 exactly; parent/rollbackf3221398d46d16bf4289e9236962e179193c3738. Railwaye94d9147-584c-4a9b-8b16-42dad60e07f4 andVercelBcxbhcDiaMXETW16WaE5sXLU2sYf SUCCESS forsamecommit. DirecturllibRailwayquery403; usedinstalledauthenticatedRailway5.49.6CLI read-onlyquerysuccessfully, no configmutation.
- Prepush backup14:46:20Bangkok SHA2564b4ddc0b6dbe2724ecc227af8f6a3efc46ae8ec56a5c459879baead6d9ad57c7 (221544bytes), restoredauto_ready_20260909t074619z verified26tables/revision; unchanged vs11:10testeddata. Seed/resetfalse beforepush. Postdeployproduction26tablehashes/counts unchanged, health/ready200,15anonymousinvoiceGETs401 acrossdirectAPI/bothaliases. No financialwrite orhistoricalrepair tosmoketestproduction.
- ExistingrealAdmin/resident28/95sessions reloaded andread invoices8total/6paid/2unpaid600each; Mayreceipt600remainsvisible andSeptemberpendingexcluded. Resident crosshouseGET house_id97 returned403 inRailwayapplicationlog07:49:58UTC. Browsernavigationreportedblockeddisplay, so usedserverlogratherthanassumingstatus; returnedtabtorealresidentpayments afterward. No freshLINElogin/R2downloadtest inthisrelease.
- Automaticfuturetriggers nowactive but existingallocationhistorynotrewritten. Recomputeanyhistoricalrepairplanfromcurrentproductionfirst, becauseordinarynewreceipt/invoiceactions canconsumeoldunusedfunds. Rollbackcode doesnotundoallocations; neverrestoreoldDBovernewtransactions/memberships. Seeautomatic-production-release.md. Contextdocumentscommittedafterappreleasepreserveexactapplicationcode.

### Final Local acceptance and fresh DB backup before release approval
- Owner approved continuing the stated Local-first plan. Ran9additional synthetic lifecycle scenarios through separate running HTTP backend8011 on cloneui_lab_20260909_073922: receipt-before-bill, bill-before-receipt, confirmation retry no writes,1500.24across3bills,600paid200fullcredit400/repeatreject,20000credit5000payment10000overcapreject, pending/reversal excluded, olderunusedFIFO/originalallocation unchanged, crosshouse isolation. All pass; all original157-house rows retained verbatim. These scenarios use clearly labelled local synthetic Statement/Pay-in fixtures, not actual bank import or real-money confirmations.
- User's Local UI database and source26tables unchanged. Stopped only separate acceptance backend, retained user5175/8010/55440 services. Existing101regression-tested application/test hashes unchanged; no additional application edit needed in this readiness round.
- Fresh productionREADONLY logical backup at2026-09-09T07:40:15.223902Z (14:40:15Bangkok), SHA256f28484666fe736078d412d90a2f522570df681b830551db101dbd7a1f5cb1ec2,221544bytes. Isolatedrestoreauto_ready_20260909t074014z matches26table counts/hashes andAlembicp5_1_notifications. No tables changed vs11:10tested snapshot. Savedautomatic-ready-current.json separately, preserving priorbackup pointers. Does not includeR2objectbytes.
- Prepared concrete release candidate/readiness report for owner/consultant. No commit/push/deploy, config/productionDBwrites, or historicalreallocation. Release approval must include future-trigger use ofoldunusedfunds; no startup sweep. Recheckfreshdata/currentcode/seedfalsebeforepush, read-onlyproduction verification first; code rollback doesnot undo newerallocations, neverrestoreoldDBovernewtransactions/memberships.

### Local UI and HTTP testing exposed active invoice read access gap
- Owner requested safe continuation and practical Local PC testing. Prepared private standalone harness outside repo/OneDrive using a new ui_lab database cloned from verified evidence. Financial/source data preserved during setup; only copied users credentials changed for local login. No production env or integrations, loopback-only ports5175/8010/55440, restricted DB role, exact-DB guard, external Python network and browser resource blocking. Local login exercises password and selected-house logic, not LINE OAuth. Old R2 slip bytes are unavailable.
- Real browser Admin and resident28/95 pages work. Real HTTP to running app created157bills/313allocations/181012.56 then retry0. Admin/resident invoice responses equal; all157May statement payloads unchanged (HTTP generated_at metadata excluded intentionally); old allocation rows/source tables unchanged. Source evidence26hashes unchanged; lab DB role cannot read evidence tables. Kept completed trial DB and prepared a fresh clone for owner hands-on testing.
- Found P1: active invoice list/single/detail/payments/allocatable-ledgers returned data without authentication, unlike the already guarded legacy accounting invoices endpoint. Added auth/selected-house/active-membership guards for reads and Admin/accounting restriction on allocatable picker. Omitted resident list filter resolves only selected token house; other-house/no-context requests denied. No money/date/allocation changes in this access fix. Five PG tests failed19subcases before fix, then passed; full74PG+27regressions pass101 total including20existing races. This is scoped verification, not a claim all system routes are access-audited.
- No production connection/write, commit/push/deploy. Latest candidate differs from previously packaged automatic code by read-access fix/test; review new local-ui-validation report before release. Local harness must NEVER be shipped to production.

### Automatic allocation recurrence fix — local implementation, not deployed
- Owner approved local code/tests after confirming residents must never choose invoices. Shared automatic_allocation service uses eligible unused household receipts ordered received_at/id and unpaid invoices ordered due_date/id, including manual bills. Called by receipt confirmation/legacy accepts and invoice generation/creation; legacy FIFO also uses the shared engine. Keeps existing Admin explicit override and restricts legacy invoice writes to Admin/accounting.
- Receipt and invoice creation acquire common transaction-scoped household advisory gates, then all ledger IDs before invoice IDs. Monthly batches take gates for all houses before row locks. Caller commits once; refresh balances after locks, expire used-ledger payment collections after inserts to prevent double spending when reusing a prepared scope. Existing allocation links are never automatically reversed/reassigned by this engine.
- 96 regressions pass:69PostgreSQL (24new) +27status/date/export.20real blocking races across suites. Two initial failing tests reproduced money received before bill creation and older unused money ignored by new confirmation. No new frontend build/live production test claimed.
- Actual services on disposable clones of verified11:10:13Bangkok backup match independent FIFO across157houses: existing-bill sweep263allocations/153600.24THB; newSeptember157bills/313allocations/181012.56THB. Both repeat idempotently; original allocation rows/invoice terms/receipt evidence unchanged; all157May cash statements unchanged. Original26-table evidence DB preserved. These figures are simulations, not production changes.
- Release impact: no startup sweep, but next new invoice or confirmed receipt consumes old unused funds too. Monthly rerun with no new bills does not sweep. On current28/95snapshot, unchanged prior links mean May unused600 would settle oldest outstandingJuly; chronological historical repair remains separate and must recalculate current preconditions before execution. Do not claim May history already repaired.
- Local changes only; no commit/push/deploy, production DB/config/R2 writes. Backup has verified DB contents and owner's new membership but not R2 object bytes; fresh DB backup required before production work. Code rollback to currentf322139 does not reverse future allocations. See implementation review for evidence and release limitations.

### Owner rule: resident cannot choose invoices; preserve and complete automatic FIFO
- Owner explicitly clarified residents have no invoice-selection right and the system must allocate automatically. Earlier discussion of payment intent/options must not become a resident invoice-picking workflow. BankStatement establishes receipt amount/time; established systemFIFO determines settlement order. Preserve automatic behavior; an Admin review of historical correction is a separate action.
- Current residentMobileSubmitPayment submitsamount/paid_at/slip, noinvoice_id; AdminPayIns callsbankReconciliation.confirmAndPost. confirm-and-post stillautoallocates butonlynewledger; ifnoinvoice itretainsunusedmoney. AccountingService.auto_generate_invoices creates/commitsbills withoutconsumingunusedreceipts. Legacyaccept paths createledgerwithoutallocation andlegacyautoapply operatesoneledger; sorter differscycle/id vsduedate/id acrosspaths. These are recurrence gaps, not a reason toremoveFIFO.
- ASTcomparison confirms auto_generate_invoices andserviceaccept_payin unchangedbetweenf367d88andcurrentrelease. Do not blame their missingtriggers on67e7a57 or claim thisrelease fullyfixedautomaticsettlement. Code review is not yet runtimevalidationofnewtriggers.
- Revised order: ensurehouse-scoped automatic use of eligibleunusedfunds whenbillsarecreated andwhenreceiptsareconfirmed; preserveactualStatementdates/caps/audit andnormalexistingallocations; verifyduplicate/retry/concurrentreceipt-and-generation scenarios onbackup; onlythenreviewhistoricalcorrectionfor28/95andotherhouses. No residentchoice; no unattendedautomaticrewritingofsettledhistoryonnewreceipt. Prior2scenarioall-houseproposal retainedasreviewevidence, notapprovedrepairinstruction. No appcode/productionmutation/commit/push inthisclarification.

### All-house allocation proposals — current backup, conditional scenarios only
- User authorized preparing the next reconciliation stage and explicitly asked to include other houses. Snapshot11:10:13Bangkok, SHA2562fe76c29c123dd58012bfe57a26063caac1fed55b15d55ce7cb286e791d4f2c0; restored26tables including owner'snew28/95membership. Original pre-release backup pointer retained separately. Production READ ONLY; all simulations use isolatedlocal allocation_review database, no productionwrite option.
-157houses:62unused+debt /2unusednodebt /90debtnounused /3settled.20of62have older unused receipts while newer receipts are allocated;42do not.53can cover presentdebt fromunused,9partly.283POSTED receipts verified throughacceptedpayin,confirmedPOSTEDStatement and rawamount/time,0sourcefailures/overcaps/crosshouseallocation. This does not prove intendedinvoiceor validate every slipimage.
- Proposedunused-only FIFO adds153600.24 toallocations across62houses: debt518797.10→365196.86,unused320818.60→167218.36; cashreceipts563619.10 unchanged. These are conditionalreviewfigures, not authorization or proofthatall62arewrong.7housesmanual/credits/reversals,5purpose-notehouses,39importedoldsystemhouses,9withpendingevidence,overlappinggroups.16pendingpayins acrossallhouses remainexcluded.
-20chronology proposals retainunchangedallocationrows andonlyreverse changedlinks; all-house hybridrehearsal332newrows/57reversed/314invoices/120ledgers vsunused-only263new/0reversed/258invoices/68ledgers. Both passed independentSQL caps/all157balances, preservedsource tables/existingpaymentfields; rollbackrestored26hashesandsequences. NegativeIDslocalonly avoidsequenceeffects. Not productionexecutiontool, no productiontransactionwrites.
-28/95pilot remainsledger187→invoice881,223→1038,269→1195,600each; retainJanuary–April, markpayments286/362REVERSED notdeleted,create3newaudit-linkedallocations atactualexecutiontime. Debt1200→600,unused600→0; PayIn317600pending requiresreviewbeforecollection. Awaitconcretepilotapproval beforeproductionrepair; one-houseapproval mustnotexpandtoall62.
- Additionalchronology inversions4houses28/24,28/39,28/56,28/103; firsttwooutside62andnotincludedinchanges. Inversionisnotproofwrongintent. Existingpayins.pyapplyFIFO stillfiltersstoredISSUED/PARTIALLY_PAID; earlierR2summary mustnotclaimallFIFOpathsusecanonicaldebt. Thisreviewusesamounts+duedate,id directly anddoesnotcallthatlegacyendpoint. Seeall-houseallocationreviewreport. No appcode/commit/push/deploy.

### Owner-confirmed membership change after backup
- After the completion report, owner explicitly confirmed adding themselves to house28/95 to inspect its data after backup. This explains the two membership-table additions at10:42:31 Bangkok; attribution comes from owner's statement, not HTTP logs. Preserve membership. Final comparison had24unchanged tables including all financial tables, with only one new row each in house_members/resident_memberships and no modified/deleted pre-existing rows. Initial all26unchanged comparison was earlier; do not confuse these two observation times. Recorded locally without another commit/push/deployment or DB write.

### Approved production release — deployed, DB unchanged
- Owner explicitly approved final commit/push. Release `67e7a57eee1f8053b8d269e66f1a1d46b25580f9` matches approved tree `73a8745ead07921439beeca850800211ee16f6aa`; parent/rollback `f367d8832c266ad9c9010bd1fb04a1b0fcafcab6`. Railway and Vercel deployment statuses SUCCESS for this exact commit.
- Refreshed pre-push backup at10:31:18 Bangkok:26public tables restored and fingerprint-verified, SHA256 `34852a2efcd59d27766fdbca99cf51e9e8626dd683bd518f9ae0a661d7068105`. Post-deploy read-only snapshot comparison shows no changed public table. Alembic remains p5_1_notifications. No financial/source-data/allocation writes; R2 attachment bytes are outside this DB backup.
- Health/ready200;6accounting alias requests plus legacy invoices require authentication401. Both frontend domains serve identical JS. Initial literal local/prod asset comparison failed because build URLs differ and Windows source embeds CRLF; exact text matches after only these known transformations. Do not claim byte-identical local/CI builds or dismiss unexplained asset differences.
- Browser checks reuse existing Admin/resident sessions: Admin invoices load; resident28/73 has8monthly invoices totaling4,800THB, independently matched to live SQL. This does not exercise a new LINE OAuth login or all production resident report downloads; all-house access/download coverage remains the pre-release restored-DB matrix.
- Keep seed/reset false when rolling back code; never restore an old DB over new transactions. Allocation repair28/95/62houses remains separate. See production-release report; previous preparation entries describe historical pre-deployment state.

### Final release — seed/reset disabled and report identity corrected
- Owner said continue after the concrete seed/reset proposal. Applied only RUN_PROD_SEED=false and PROD_RESET_ADMIN_PASSWORD=false via Railway variableCollectionUpsert with replace=false/skipDeploys=true. Read-back at10:25:43 Bangkok confirms false/false, all other variables unchanged, active deployment unchanged7c6d5c98-918b-4744-b3cc-ab4de9d4454d. No restart/code deploy, no financial writes. Keep these flags off during any later rollback.
- English name/contact details were not supplied; used known Thai community nameหมู่บ้านแมกไม้ลีลาวดี and neutralกรุณาติดต่อฝ่ายบัญชี/นิติบุคคล. Config default English empty; both PDF/XLSX join only nonempty project names. Removed fictitious phone/email defaults. Only these report default fields in core/config.py changed, not DB/auth settings.
- Final verification72 regressions passed, plus2,532 real-app read/access/download checks on fresh release_verify backup,157Excel header assertions; Admin/resident parity retained. Reviewed4PDF pages. All26restored-table fingerprints unchanged. Source/restore Alembic head p5_1_notifications. Financial R2 tests still include13real blocking races.
- Final reviewed package is release-candidate-20260909-final, separate from retained R1/R2 evidence. RealGit index untouched; no commit/push yet. See final-release review for exact scope/backup/rollback and remaining live LINE/mobile checks. No production allocation repair.

### Release preparation — fresh backup and verified live startup flags
- At10:16:13 Bangkok exported a REPEATABLE READ READ ONLY production snapshot, pg_dump --snapshot, then restored to isolated local release_verify_20260909t031612z.26 table counts/hashes match; Alembic p5_1_notifications; no table changed relative to R2 tested snapshot. Backup SHA2567b05ab759eb38b7a11e20162af174a1109d305de91f575fe4baf4ab700e273bb,221503bytes. Old backups and steps12-current retained.
- Initial cross-host hash comparison failed with equal row counts because ORDER BY used differing collations. Correct cross-platform check specifies COLLATE "C"; new same-snapshot backup verified. Do not mistake ordering-dependent hashes for data loss or simply suppress failures.
- Railway CLI authenticated read confirms RUN_PROD_SEED=true and PROD_RESET_ADMIN_PASSWORD=true; credentials are present. This makes startup reseeding/credential reset a concrete pre-release risk. Prepared false/false variableCollectionUpsert with replace=false/skipDeploys=true but did not execute. Other secrets untouched. Railway API direct Python403 worked through official CLI; no need to ask for tokens or expose them.
- PROJECT_NAME_TH/EN and ACCOUNTING_CONTACT unset; defaults would print generic village and fictional contact. Asked owner for actual English name/contact (Thai name known). Release must resolve header/config before distributing statements. No project name/contact invented.
- Source/test27 hashes match packagedR2, GitHub/local/activeRailway f367d88, health/ready/frontend200. New snapshot matches prior tested data so no duplicate exhaustive matrix needed. Startup, dependency and Docker source unchanged; Dockerfile effective in active deployment. No application edits, production config changes, financial writes, commits or deploys this pass. See release-preparation review for concrete next actions.

### R2 advisor condition — fresh production eligibility census, read-only
- At10:09:33 Bangkok queried live production with SSL, REPEATABLE READ and transaction_read_only=on; rollback at completion.283 POSTED ledgers passed shared allocation_error,0 rejected; POSTED no-pay-in count0, eligible0, rejected0. Independent SQL matches. Helper source hash verified against R2 manifest. This closes the requested current-data exact-match question, not future eligibility monitoring or whole-system certification.
- No tolerance relaxation or source timestamp/amount repair. Future rejections require a recorded ledger/bank/batch investigation and separately approved audited correction; absence from picker is never proof of no receipt. Procedure documented in `docs/reviews/2026-09-09-r2-live-eligibility.md`.
- Correct reviewer overclaims: pending-exclusion flag is descriptive (separation is enforced by actual calculation/tests);7 guarded handlers do not certify global authorization or prove successful production disclosure; deployment startup can write depending on flags. Roll back exactrelease code without replacing liveDB or resetting history.
- This pass changes documentation only, preserves R2 package, and does not commit/push/deploy or repair allocations. Private evidence20260909T030934Z-r2-live-eligibility.private.json remains outsideGit. Fresh production backup/startup checks/release approval remain required before deployment work.

### R2 — completed locally after owner authorization
- Legacy Aging now subtracts ACTIVE payments by receipt date and applied credits by creation time, with Bangkok exclusive-next-midnight bounds; no stale-status filter. Current Aging/FIFO use canonical outstanding. September debt518797.10 agrees with independent SQL, removing1200 overstatement. Historical Aging is restated, current Aging uses as_of only for age buckets; neither is a frozen close.
- Invoice picker/apply use shared receipt eligibility. No-pay-in ledger requires POSTED bank receipt, CONFIRMED batch, no conflicting matched pay-in, matching amount/time. Ledger-first locking remains; caps and reversal are covered by three new real blocking tests. Existing accepted pay-in branch remains supported and checks house parity. Financial writer changes must be disclosed in release review.
- Correction to earlier inference: confirm_and_post's nullable payin_id expression is not an accessible Case B creation path; target_house_id guard rejects no-pay-in before insert. R2 does not change this creation flow. Snapshot still has zero POSTED Case B; no attribution of62-house backlog to this possibility.
- Cash Flow confirmed receipt source is now POSTED IncomeTransaction (Decimal aggregation, Bangkok dates). Existing AR timing policy is preserved. Legacy invoice-list access now selected-house + ACTIVE ResidentMembership. Village income activity excludes REVERSED and uses Bangkok; frontend Cash Flow Jan1 default no longer shifts into prior year.
- Pending PENDING/SUBMITTED evidence appears separately with current/all-period scope in monthly/range JSON, PDF/Excel and resident mobile summary. Never add it to confirmed money; pending history includes SUBMITTED. Local PDF uses default village/contact Settings: verify live configuration before distributing documents.
- Verification72 regressions (PG45, including13 blocking races; status12; paid-at7; export8), build/download/R2 component harness, restored-data40,673 checks all pass. Independent R2 SQL compares1,413 house-month aging rows and all-house pending data. All public-table fingerprints unchanged; R1 evidence retained and R2 outputs separate. Private DBbackup checksum retained; R2 object bytes are not included. No production write, commit, push, deploy or allocation repair.


### Advisor review — revised release recommendation with measured impact
- Rechecked advisor findings against code and read-only pristine restore. Legacy September Aging519997.10 vs canonical518797.10 gives1,200 overstatement. Active UI uses a different Aging endpoint which already subtracts credits, but shares stored-status filtering risk. Case B is excluded from both allocatable list and main apply API; accounting/bank FIFO can handle it, so a full fix spans writer validation and tests. Snapshot contains0 POSTED no-pay-in ledgers and0 positive-debt invoices filtered out by status; neither establishes the cause of62-house unused-money cases.
- Confirmed ineffective baseline async guards, with disclosure/exploitation unproven (baseline report service calls fail on missing CreditNote attributes). Confirmed timezone defaults/activity formatting; recent activities also include reversed ledgers. Pending-slip context should be shown separately from confirmed amounts. Credit creation date and restated semantics are separate decisions.
- Full R1 release recommendation paused for a revised correctness/security scope; no code changes or production mutation in this consideration pass. Do not claim deployment cannot write DB merely because financial writers are unchanged: startup remains a separate precondition. Detailed accepted findings, qualifications and next work: `docs/reviews/2026-09-09-advisor-adjudication.md`. R1 package preserved; working-tree documentation differs and needs R2 after fixes.

### Final accumulated diff review — test the real application mount
- Found a release-blocking test blind spot: prior fixture mounted accounting under `/api`, but `main.py` did not. Added a dedicated six-GET `/api/accounting` report router; original `/accounting` paths remain, and legacy writer APIs are not exposed under the new prefix. Added actual `app.main` routing/auth/download regression; reran the restored-data HTTP matrix through the real app and middleware (2,261 passes, fingerprints unchanged).
- Regression60 includes PG33 rerun after routing fix. Financial writer ASTs remain unchanged; no DB-model/migration/dependency/deployment-config changes. Alembic head in restored production and code both `p5_1_notifications`; remote master verified `f367d88`. Docker Desktop is unavailable, so container build is not claimed. Live seed flags and real LINE/mobile checks remain explicit release conditions.
- Release proposal/rollback documented in `docs/reviews/2026-09-09-release-candidate.md`; exact patch/source/hash manifest in private `release-candidate-20260909-r1`. No Git commit/push or production mutation. Code rollback must not restore an old database or erase new resident payments. 28/95 and bulk allocation corrections remain separate.

### Resident report access and monthly download — local integration completed
- Owner authorized the previously proposed follow-up. Six house-report routes now enforce authenticated selected-house context plus ACTIVE ResidentMembership/house; no arbitrary DB house fallback. Admin/accounting retain access. Global authentication and unrelated legacy routes are unchanged. Real signed-session tests cover current `/api/auth/select-house`, switching, membership deactivation, expiration and session revocation.
- Fixed active Houses download URL to monthly `/statement/house/{id}` (old URL was date-range endpoint) and current month to Bangkok. Monthly PDF uses bundled Sarabun regular/bold, escaped/wrapped names, repeated headers and real page numbering; files use numeric house IDs. Reviewed May28/95 PDF and all3 synthetic pagination pages visually.
- Restored production user/membership records tested via actual HTTP handlers using a random local signing key:114 membership records,111 eligible house selections,684 report access outcomes,222 resident downloads,111 resident/Admin matches; Admin JSON/PDF/XLSX for157 houses.2,259 checks pass; all public-table fingerprints unchanged. Regression59 and frontend handler harness/build pass. Details `docs/reviews/2026-09-09-report-access-download.md`.
- This does not constitute real LINE OAuth/mobile browser or production deploy testing. No production mutation, invoice reallocation, migration, commit/push/deploy. Code release must be reviewed separately from any evidence-approved data correction.

### Approved follow-up — legacy read reports repaired on pristine restore
- Corrected five `AccountingService` read functions using actual CreditNote schema and shared `accounting_reports.py`. Invoice balance counts ACTIVE allocations; dated cash statements count POSTED receipts including unused funds once, applied credits, and invoices by issue date. Never use allocation dates to relocate actual receipt cash. Period reports use Bangkok bounds, Decimal running balances and exact mid-month cutoffs; no query-error-to-zero fallback. These are restated views of currently valid records, not locked historical snapshots.
- Added five local PostgreSQL regression tests including actual HTTP router calls with dependency-injected users. Fixed existing house-guard calls (await/order), nonexistent monthly House fields and resident credit summary key. Modern ResidentMembership/token integration is not validated by legacy HouseMember fixtures; do not claim full resident permission coverage.
- Validation: previous matrix22,597 pass/0 fail; independent raw SQL13,806 pass/0 fail (157 houses,1,413 house-months,471 ranges,nine aggregate months);57 regression tests pass. Pristine restore public-table fingerprints unchanged. Financial writer ASTs unchanged versus HEAD. No production mutation, allocation repair, commit/push or deploy.
- May28/95 now appears in local cash report as receipt187/600 THB (closing0), without changing allocations. Code changes do not resolve the production invoice mapping by themselves. Remaining UI work includes monthly download URL mismatch and legacy PDF Thai font fallback. Full evidence/limits: `docs/reviews/2026-09-09-legacy-read-followup.md`.

### Steps1–2 executed — all-house evidence inventory and code parity on pristine snapshot
- Owner authorized only inventory/testing before production. Created a new full PG17 backup and restored to `steps12_20260908_185825`; SHA256 `1b5f58eb0bb2dc3d3e107f57e933de8580a04c66dddefe9c57234cf90ed27063`. No planned 28/95 correction applied to this snapshot. Compared git-archived deployed HEAD f367d88 and working tree against the exact same database with enforced read-only sessions; all public-table fingerprints remained equal (canonical UTC serialization).
- Detailed report: `C:\Users\sanch\moobaan-db-backups\20260909-2895\steps12-20260908T185813Z\review-report.html`; aggregate review in `docs/reviews/2026-09-09-production-readiness.md`. It contains157 classified household rows and283 POSTED receipt references. All283 match stored original Statement raw_row amount/time, bank entry, confirmed batch and accepted pay-in. No source errors. Corpus286 ledgers includes3 reversed ledgers, so283 is the correct active receipt count.
-62 households need allocation review (53 full coverage,9 partial coverage;9 have pending slips).2 have unused money with no invoice debt,90 have invoice debt without unused confirmed receipts,3 have neither. Totals: confirmed563619.10 = applied242800.50 + unallocated320818.60; billed763197.60 − credit1600 − applied242800.50 = invoice debt518797.10; net due after all confirmed receipts197978.50.16 pending/submitted slips total12599.19 are excluded. These categories do not establish a household's intended allocation or advance/refund intent.
- Parity testing found removing REVERSED sums alone exposed the village dashboard's missing credit subtraction: the two old errors coincidentally offset in this snapshot. Candidate now uses the invoice remaining-amount method for village debt/count and Admin/resident open-invoice counts; pending-slip count is real. Cash Flow now bounds/groups timestamps in Bangkok (deployed UTC run incorrectly put one receipt in May rather than June). These are read/display changes; no status rows or ledger data rewritten.
- Final candidate:22440 comparisons/reads pass;1256 calls fail solely in5 pre-existing legacy accounting functions across157 houses (month snapshots tested4 periods). Baseline4606 failures. No candidate-only failure in this matrix. Legacy CreditNote.amount/house_id and date/ledger/fallback problems remain explicit release blockers; see review document. Regression52 passes (PG25 including10 real lock races, status12, paid-at7, export8), frontend loader/timezone harness/build pass. This is not full production HTTP/browser/permission coverage. No commit/push/deploy or production correction performed.

### Whole-system read-only audit after owner clarified 28/95 is only an example
- At 2026-09-09 01:52 Bangkok, audited live production using PostgreSQL REPEATABLE READ READ ONLY (`transaction_read_only=on`):157 houses,1259 invoices,286 ledgers,405 ACTIVE and5 REVERSED allocations. Private detailed report: `C:\Users\sanch\moobaan-db-backups\20260909-2895\20260908T185232Z-system-audit.private.json`.
- Zero findings in these specific checks: invoice paid+applied-credit above total; ledger ACTIVE allocations above amount; ACTIVE allocations against REVERSED ledger; posted ledger without linked bank; posted bank/ledger amount or received-time mismatch; posted ledger/pay-in amount/status/house/bank-link mismatch; cross-house ACTIVE allocation; ACCEPTED pay-in without POSTED ledger; POSTED bank credit without POSTED ledger. These checks compare stored bank rows to financial records and do not establish exhaustive source-file, authorization, or UI correctness.
- Found75 posted receipts with remaining unallocated value, across64 houses, total320818.60 THB. Of those,62 houses also have outstanding invoices; their unallocated money totals254218.60 THB. Must classify advance payments/explicit allocation intent/unapplied historical receipts before applying any batch correction. Unallocated does not by itself mean corruption.73 of75 source ledgers were created before September; creation date does not prove when they became unallocated.
- Found two stale stored invoice statuses: IDs5 (28/5) and111 (28/112), total600 each, credited600, paid0, storedISSUED. Canonical list correctly returns CREDITED; stored-status-based counts and legacy debt sums can disagree. Existing dashboard debt sum also omits credit subtraction. These findings are independent of the confirmed 28/95 receipt source.
- Commit b645e0e's invoice date feature adds a read-only computed received_at field and eager loading; that patch contains no migration or receipt/allocation rewrite. Evidence supports investigating historical allocation backlog exposed by the new display; does not certify all recent finance changes as defect-free.
- Owner clarified full-system correctness is required. Prepared house28/95 repair remains a tested example, not a complete system remediation. No production data correction or deployment has been performed. Remaining release work includes global reconciliation classification, shared Admin/resident/report semantics (received/unallocated/applied/pending are distinct), regression verification beyond this one house, and post-deployment live verification.

### Statement source verification and resident parity — local restored copy
- Owner explicitly reaffirmed uploaded bank statements as the authority for receipt amount/time. Checked original `raw_row` dates/times/credits and all seven linked house28/95 bank→ledger→pay-in records. Seven confirmed receipts total4200 THB; receipt timestamps equal bank effective_at, amounts agree. Uploaded Jan26.csv–Aug26.csv batches are CONFIRMED. Sender-reference search across uploaded statements found these same seven records; it does not prove absence of payments from a different sender/reference.
- Additional receipt PayIn317, 600 THB, dated Sep8 is PENDING; no September statement has been uploaded. Keep this pending evidence unchanged, do not book it or claim the resident has not transferred money. Mobile dashboard's separate blocking check/latest-payment timeline must retain this pending submission.
- After rehearsed correction, directly executed the resident dashboard API with local test house context: confirmed income4200, billed4800, current_balance=-600, invoice outstanding600, one unpaid invoice. Admin/resident pay-in list payloads for house96 match exactly (8 records: 7ACCEPTED+1PENDING). Shared invoice API is used by both UIs. These are local API checks on the restored production copy, not logged-in resident live-browser verification.
- Found shared resident pay-in formatter relied on device timezone; set Bangkok explicitly for date/time. No source DB timestamp edits. Repair script now additionally refuses bank/ledger timestamp mismatch, bank/pay-in link mismatch, or unconfirmed source statement batch.

### INC-20260909-2895-MAY — receipt reconciliation preparation
- Direct production read confirms house 28/95 (id 96) receipt PayIn 204 / ledger 187 exists, POSTED, 600 THB, received 2026-05-11 00:05Z, with matched bank evidence intact. Ledger creation 2026-06-08 13:29:20Z precedes May invoice 881 creation 13:32:33Z by 3m13s. Thus there was no May invoice when the money was posted. Exact historical API route is not established; do not attribute this conclusively to the legacy accept route.
- Subsequent receipts ledger223/PayIn221 and ledger269/PayIn273 were allocated to May/June. Owner approved mapping ledger187→May881, ledger223→June1038, ledger269→July1195, 600 each; August1352 remains outstanding. Preserve original payments286/362 as REVERSED, add new ACTIVE allocations, append an incident audit note to affected invoices. Never change source receipt date, amount, bank match, slip, or accepted status to repair allocation.
- Full PostgreSQL17 custom backup `before.dump` (221503 bytes, SHA256 `ff364c7670b1402dce6af7b09b4c3ed4094087a6a6a4457372c137401e5e738f`) restored with exit-on-error into isolated password-protected localhost:55440. Backup, scripts, and full private evidence journals live outside Git/OneDrive at `C:\Users\sanch\moobaan-db-backups\20260909-2895`, ACL restricted to owner/SYSTEM. This backs up DB and evidence references, not R2 object bytes.
- Repair rehearsed with rollback, then committed on the restored copy; idempotent retry verified. Serializable transaction locks ledgers then invoices, refuses unexpected payment/credit/period changes, retains source rows byte-for-byte, checks fingerprints of all public tables, and durably writes the journal before commit. Production correction has NOT run at time of this entry.
- Prerequisite fixes: all InvoicePayment amount sums filter ACTIVE; both invoice payment-history APIs expose status; UI marks reversed allocations without hiding their source evidence; filtered house view warns about accepted receipts with unused funds and handles lookup failure/stale responses. Allocatable-ledgers excludes REVERSED income and eager-loads payments/house. No automatic historical reallocation or schema migration.
- Verification: PostgreSQL23 (including reversal totals/audit and allocatable ledger tests), paid-at7, export8, frontend loader notice/failure/race harness, frontend build. Pre-existing legacy accounting methods additionally fail on absent CreditNote.amount/house_id; this separate issue remains and broad statement-report correctness is not claimed.

### Codex follow-up review fixes — local, not committed/deployed; base `f367d88`
- Owner authorized fixing the three review findings. Export now holds the filter snapshot from the latest successful table request; typing a new house does not change export before searching. Unknown house, pending request, or failed load disables export. Request sequencing prevents older responses replacing newer table/filter state; clear explicitly reloads without the house/status filters.
- PDF looks first in `backend/assets/fonts/Sarabun-Regular.ttf`, which Docker already copies. Verified actual Thai text and Baht glyphs in rendered multi-page PDF; table header repeats. Generated timestamps/filenames now explicitly use Asia/Bangkok. Regression test requires Sarabun embedded, not just a `%PDF` signature.
- GET `/api/invoices/{id}` now returns canonical status, paid/outstanding/credit/net amounts, receipt timestamp, and manual invoice type/cycle/reason consistently with list/detail; no historical database rewrite.
- Verification: PostgreSQL suite 21 passed (including new single/list/detail consistency test), export suite 8 passed, frontend actual-loader harness checks applied filters/missing house/clear/out-of-order responses, and `npm run build` passed. PDF QA uses synthetic rows in a temporary directory. No production DB or live writes. Test PostgreSQL stopped after verification.

## 2026-09-08

### Phase 3 — export ใบแจ้งหนี้ให้ตรงหน้าจอ — `6eacde6` (deployed + owner ยืนยัน Excel ใช้ได้)
- **เดิม export มี 6 คอลัมน์** ที่ไม่ตรงตาราง: ไม่มี ชำระแล้ว/เครดิต/ค้างชำระ, สถานะเป็น enum ดิบ, จำนวนเงินเป็น "ข้อความ", วันที่เป็น UTC, และ**ไม่ส่ง filter ที่ผู้ใช้ดูอยู่** (export ทั้งหมดเสมอ)
- **แก้:** Excel 11 คอลัมน์ (เพิ่ม ชำระแล้ว / เครดิต-ลดหนี้ / ค้างชำระ / วันครบกำหนด / วันที่ชำระล่าสุด) + สถานะไทยจาก `get_settlement_status()` → อ่านตรงกับหน้าจอ
- **เปลี่ยน "วันที่สร้าง" (`created_at` = เวลาระบบบันทึก record) → "วันที่ออกบิล" (`issue_date` = business date)** — ตัวหลังคือสิ่งที่รายงานบัญชีต้องการ (owner ยืนยัน)
- **จำนวนเงินเป็นเซลล์ตัวเลข** (`number_format '#,##0.00'`) → Excel รวมยอด/กรองได้; PDF ยังเรนเดอร์เป็นข้อความบาท
- **วันที่/เวลาเป็น Asia/Bangkok** — `fmt_date`/`fmt_datetime` เดิม `strftime` บน UTC ตรงๆ (latent bug ที่ซ่อนอยู่ใน export)
- **PDF ใช้ชุดย่อ 8 คอลัมน์** ผ่าน `drop_columns()` (A4 ใส่ 11 ไม่พอ) — Excel เก็บครบ; owner ระบุว่าไม่เน้น PDF
- **บทเรียน:** เพิ่ม/แก้คอลัมน์บนหน้าจอเมื่อไร **ต้องเช็ก export ด้วย** ไม่งั้น 2 ที่ drift; และปุ่ม export ต้องส่ง filter ปัจจุบันเสมอ (ไม่งั้นได้ไฟล์ที่ไม่ตรงกับสิ่งที่เห็น)
- เทสต์: `backend/test_report_export.py` (8 เคส — tz, เซลล์ตัวเลข, PDF trim + remap money index, สถานะไทย)

### Phase 2 — canonical settlement status ที่เดียวใช้ทุกที่ — `abbcaa7` (deployed + ยืนยันใช้ได้)
- **ปัญหา:** สถานะถูกคำนวณ 3 ที่และไม่ตรงกัน — UI เช็ค `outstanding === 0` ก่อน ทำให้**บิลที่จ่ายครบโชว์ "เครดิตแล้ว"** และ**บิลลดหนี้บางส่วนที่ยังไม่จ่ายโชว์ "ชำระบางส่วน"**; filter ยังส่ง `CREDITED` เข้า enum PostgreSQL ที่ไม่มีค่านี้ (เสี่ยง query error)
- **แก้:** `Invoice.get_settlement_status()` เป็นกฎเดียว (CREDITED/PAID/PARTIALLY_PAID/ISSUED); `update_status()` persist ตามนั้น — **DB เก็บ `CANCELLED` สำหรับเครดิตเต็ม เพราะ enum ไม่มี CREDITED** (CREDITED เป็นค่าระดับ API)
- รวม money helper เป็น Decimal ชุดเดียว — `get_remaining_balance_decimal()` เป็น **single source ของ credit cap** (credit note ใช้ตัวนี้ แทนสูตร inline)
- list/detail/apply-payment ส่ง canonical status; filter validate แล้วกรองบน canonical แทน enum ดิบ; eager-load house/credit_notes ปิด N+1 ที่เหลือ
- **UI เลิกคำนวณสถานะเอง** — `formatStatus`/`getStatusBadge` เชื่อค่าจาก API
- เทสต์: `backend/test_invoice_settlement_status.py` (12 เคส รวม regression ของ 2 mislabel)
- **หมายเหตุการยืนยัน (สำคัญ):** รัน PG suite ซ้ำไม่ได้ (เครื่องนี้ไม่ใช้ Docker และไม่มี PG) จึงยืนยันแทนด้วย (1) P2/P3 **ไม่แตะ** `invoice_locking.py`/`accounting.py`/`payins.py`/`bank_reconciliation.py` → การล็อกที่ PG suite ตรวจไว้ตอน P1 ไม่ถูกแก้ (2) พิสูจน์สูตร credit cap เดิม vs `get_remaining_balance_decimal()` **เท่ากันทุกเคสจาก 6,000 combinations** — ถ้าจะรัน PG suite รอบหน้าใช้ PostgreSQL for Windows (EDB) ชี้ `CREDIT_TEST_DATABASE_URL` ไป localhost ได้ ไม่ต้องใช้ Docker

### Credit settlement phase 1 — Codex, reviewed by Claude — `44fc26f` (deployed + ยืนยันใช้ได้)
- Owner explicitly confirmed: credit only the outstanding debt; full credit clears outstanding after prior credits and ACTIVE payments. Base/rollback reference: `0f9d67f40141ad5719d5356db18670949330f88c`.
- Credit creation now locks the invoice, validates two-decimal finite money against current outstanding, and refreshes settlement status in the same transaction. Original invoice amount and existing historical entries are preserved.
- All current invoice-payment insert paths participate: invoice apply API, accounting service + FIFO, pay-in FIFO, bank confirm/post. Bank reversal also participates. Lock existing ledger before invoices; acquire multiple invoice locks in ID order, then allocate in existing FIFO order. Expire cached payment/credit collections after locking.
- Decimal arithmetic prevents fractional-cent residuals. Service FIFO commits once for the batch instead of releasing locks between invoices.
- PostgreSQL regression suite: `backend/test_credit_settlement_postgres.py`. Explicit localhost-only `CREDIT_TEST_DATABASE_URL`, database `moobaan_credit_test`, fresh random schema per run; verifies actual blocking through `pg_blocking_pids`, including a Session with cached old balances. No production data used.
- Verified 2026-09-08: 20 PostgreSQL 16.15 integration/concurrency tests passed, plus all 7 existing invoice paid-at tests. Changed API/service imports and `git diff --check` passed. PostgreSQL schema is built from current models in an isolated schema, not a production migration rehearsal. Docker could not start; used the EDB PostgreSQL binary distribution on localhost port 55439 instead and stopped the test server after verification.
- Existing legacy `AccountingService.issue_credit_note` is house-scoped and constructs fields absent from the current CreditNote model; it cannot successfully insert using the current model. Not redesigned in this invoice-scoped fix.
- Owner authorized Phase 1 commit/push after review. Remaining separate work: consolidate the duplicated outstanding Decimal calculation, canonical list/detail/filter/UI status (including historical stale statuses), and export specification/implementation. No historical data repair or migration in phase 1. Push/deploy verification must be checked separately; this entry does not certify production deployment success.

### feat(invoices): เพิ่มคอลัมน์ "วันที่ชำระล่าสุด" + แยกเวลารับเงิน/บันทึกเข้าบิลในประวัติ — `b645e0e` (deployed, ยืนยันใช้ได้)
- **สิ่งที่ทำ:** เพิ่มคอลัมน์ "วันที่ชำระล่าสุด" ในตารางจัดการใบแจ้งหนี้ + ปรับ payment history ให้แสดง "เวลารับเงิน" (received_at) คู่กับ "บันทึกเข้าบิล" (applied_at)
- **แหล่งเวลาที่ถูกต้อง (บทเรียนสำคัญ):** ในการชำระบิล มี timestamp 2 ชั้น อย่าสับสน
  - `InvoicePayment.applied_at` = เวลาที่**เจ้าหน้าที่ผูกยอดเข้าบิล** (record time) — ไม่ใช่เวลาชำระจริง
  - `InvoicePayment.income_transaction.received_at` = **เวลารับเงินจริง** (จากสเตทเมนต์/pay-in, not null, มี tz) ← ตัวนี้คือ "เวลาที่ชำระ"
  - ยังมี `income_transaction.bank_transaction.effective_at` (เวลาธนาคาร = source of truth) และ `payin.transfer_date` (datetime มี tz — **ไม่ใช่ date ล้วน** ตามที่เคยเข้าใจผิด)
- **helper `Invoice.get_last_payment_at()`:** คืน `max(received_at)` ของ payment ที่ ACTIVE เท่านั้น (ตัด REVERSED); คอลัมน์เดียวแทนหลาย payment ไม่ได้ จึงตั้งชื่อ "ล่าสุด" — ความหมายแม่นยำ = "เวลารับเงินล่าสุดของรายการที่นำมาชำระบิลนี้" (ไม่ใช่ "เวลาปิดยอด" เพราะเคสรับเงินล่วงหน้า/ปิดด้วยเครดิตโน้ต received_at ≠ วันปิดบิล)
- **timezone display (บทเรียน):** `toLocaleString('th-TH')` กำหนดแค่ภาษา **ไม่กำหนด timezone** → ผู้ดูต่างเขตเวลาเห็นเวลาต่างกัน; ต้องใส่ `{ timeZone: 'Asia/Bangkok' }` เสมอเมื่อแสดง timestamp (DB เก็บ UTC ตาม policy) — ยืนยันด้วย node ว่าผลตรงกันทุกเครื่อง
- **perf:** เพิ่ม `selectinload(Invoice.payments → InvoicePayment.income_transaction)` ใน list API กัน N+1 (house/credit-notes ยังมี N+1 เดิม แต่ไม่ได้เพิ่มจากงานนี้)
- **ไม่ต้อง migration** — ใช้คอลัมน์ที่มีอยู่แล้ว (`received_at`, `applied_at`)
- **เทสต์:** `backend/test_invoice_paid_at.py` (7 เคส: ไม่มี payment, ลำดับรับเงิน≠ลำดับผูกยอด, REVERSED ล่าสุดถูกข้าม, ledger หาย, status None, UTC→Bangkok) — ผ่านครบ
- **ข้อจำกัดที่รับทราบ:** เทสต์ใช้ข้อมูลจำลอง ยังไม่ยืนยันกับ DB จริง/หน้าจริงตอน review (ยืนยันบน production หลัง deploy แล้วว่าใช้ได้)

---

## 2026-07-01

### fix(bank-statements): แปลงหน้าเป็น dark theme + เติม i18n key ที่หาย — `fd4951d` (deployed Vercel)
- **อาการ:** หน้า `/admin/statements` ตัวหนังสือจางมองไม่เห็น + หัวตารางโชว์ key ดิบ `BANKSTATEMENTS.ACCOUNT` / `common.view`
- **สาเหตุ:** เป็นหน้าเดียวในแอปที่ยังใช้การ์ดสีขาว (`bg-white`) แต่หัวข้อ/เซลล์ตารางไม่กำหนดสีตัวอักษร เลยรับสีขาวจาก global theme (`:root color: rgba(255,255,255,0.87)` ใน `index.css`) → ขาวบนขาว มองไม่เห็น; ส่วนแอปที่เหลือใช้ระบบ `.card` (พื้น slate-800) อยู่แล้ว
- **แก้:** [BankStatements.jsx](frontend/src/pages/admin/BankStatements.jsx) แปลงทั้งหน้าเป็น dark theme มาตรฐานแอป (`.card`/`.input`, ตาราง slate-700, ตัวอักษรอ่อน, debit/credit → red-400/green-400) + [th.js](frontend/src/locales/th.js) เติม key `common.view`='ดู', `bankStatements.account`='บัญชี', `bankStatements.period`='งวด' (JSX เรียก key เหล่านี้แต่ไม่มีใน locale — มี `batchAccountCol`/`batchPeriodCol` แต่คนละชื่อ)
- **บทเรียน:** หน้าใหม่ทุกหน้า**ต้องใช้ dark theme** (`.card`, ตัวอักษร `text-white`/`text-gray-300/400`) ตาม CLAUDE.md "Dark theme throughout" — อย่าใช้ `bg-white` + ปล่อยตัวอักษรไม่กำหนดสี (จะรับสีขาวจาก global แล้วหายไปบนพื้นขาว); ถ้าเพิ่ม UI text ใหม่ต้องเติม key ใน `locales/th.js` ทุกครั้ง
- **verify:** `npm run build` ผ่าน (ไม่มี error)

### fix(bank-statements): month validation ต้องเทียบเวลาไทย ไม่ใช่ UTC — `14c6500` (deployed + ยืนยันใช้ได้)
- **อาการ:** อัปโหลด Bank Statement CSV เดือน มิ.ย. → ถูกบล็อก `Transaction #1 (date: 2026-05-31) falls outside selected month 2026-06` + warning `doesn't match CSV date range` ทั้งที่รายการอยู่ในเดือน มิ.ย. จริง
- **สาเหตุ:** `effective_at` เก็บใน DB เป็น **UTC** (ถูกตาม policy) แต่โค้ด validate เดือนเรียก `.date()`/`.month` บนค่า UTC ตรง ๆ โดยไม่แปลงกลับเป็น Asia/Bangkok → รายการหลังเที่ยงคืนไทย (เช่น 01:51 ICT = 18:51 UTC ของวันก่อนหน้า) ถูกตัดสินว่าอยู่คนละเดือน แล้ว reject ผิด
- **ละเมิด policy ตัวเอง:** `timezone.py` ข้อ 3 "All business month boundaries = Asia/Bangkok" — โมดูลอื่น (payins, dashboard, invoices, payin_state) แปลง `.astimezone(BANGKOK_TZ)` ก่อนตัดสินอยู่แล้ว มีแค่ 2 จุดใน bank statement import ที่ลืม
- **แก้ 2 จุด:** [bank_statement_validator.py:95](backend/app/services/bank_statement_validator.py:95) (error ที่บล็อก import) + [bank_statements.py:269](backend/app/api/bank_statements.py:269) (preview warning) → แปลงเป็น Bangkok ก่อนดึง date/month; DB ยังเก็บ UTC เหมือนเดิม ไม่ต้อง migrate batch เก่า
- **เทสต์:** เพิ่ม `backend/test_bank_import_timezone.py` (4 เคส: หลังเที่ยงคืนไทยผ่าน / เดือนผิดจริงยังบล็อก / ปลายเดือนดึกผ่าน / CSV พ.ศ. end-to-end) — ผ่านครบ
- **วิธีนำไปใช้ (ย้ำบทเรียน):** ทุกที่ที่ตัดสิน "อยู่เดือน/วันไหน" ต้อง `.astimezone(BANGKOK_TZ)` ก่อนเสมอ — อย่าเทียบ date/month บน datetime UTC ตรง ๆ
- **rollback point:** ก่อน push = `c8c8020` (ถ้าต้องดึงกลับใช้ `git revert 14c6500` ปลอดภัยกว่า force-push)
- **⚠️ พบระหว่างทาง — venv พัง (เปลี่ยน PC ใหม่):** `backend/.venv` ผูก Python 3.13 (`C:\Python313`) ที่ถูกถอนไป เครื่องใหม่เหลือแค่ 3.14 → venv ย้ายเครื่องไม่ได้ (ฝัง path interpreter เต็ม) จึงพัง ไม่เกี่ยวกับ Docker
  - **production ใช้ Docker/Python 3.11** (`backend/Dockerfile` → `python:3.11-slim`, Railway build ด้วย `railway.json` builder=DOCKERFILE) — คนละชั้นกับ venv ไม่กระทบ
  - **ทางแก้ local (เลือกทางใดทางหนึ่ง):** (1) ใช้ Docker ไปเลย `cd docker && docker compose up` (มี db+backend+frontend, Python 3.11 ตรง prod ที่สุด) หรือ (2) สร้าง venv ใหม่ด้วย **Python 3.11** ให้ตรง prod (`py -3.11 -m venv .venv` + `pip install -r requirements.txt`) — อย่าใช้ 3.14 (ใหม่เกิน dep บางตัวอาจไม่มี wheel + พฤติกรรมต่างจาก prod)

---

## 2026-06-12

### Push docs commit `3342e64` + ยืนยัน production เขียว + พบ URL backend ใน docs ผิด
- push commit docs (context files + .env.example + whitespace) → **Vercel READY + Railway แข็งแรง** (`/health` 200, `/ready` 200 database ok) — production ไม่กระทบ
- **พบ docs ผิด:** CLAUDE.md เขียน backend URL เป็น `moobaan-smart-production.up.railway.app` (มีขีด) แต่ **URL จริงคือ `moobaansmart-production.up.railway.app`** (ไม่มีขีด — ยืนยันจาก JS bundle ของ frontend production) → แก้ใน CLAUDE.md/AGENTS.md แล้ว (รอ push รอบหน้า)
- **health endpoints:** `/health` (liveness), `/ready` (DB check), `/api/system/status` — ไม่ใช่ `/api/health`
- **เครื่องใหม่:** ตั้ง git identity (`SafetyDady <sanchai5651@gmail.com>`) + เคลียร์ `index.lock` ค้าง (lock ตาย 7 ชม. ไม่มี git process)

### ตรวจ env: local ≠ production + คีย์หลุดในแชต + พบ config เสี่ยง
- **local `.env` ไม่ใช่ backup ของ production** — มีแค่ค่า dev: `DATABASE_URL`→local db, `SECRET_KEY`→ค่า dev (ขึ้นต้น `secret` ซึ่ง prod จะ reject), ขาดคีย์ integration ทั้งหมด (R2 5 ตัว, LINE 2, SMSMKT 3, OTP_*, token config). ค่าจริงอยู่บน **Railway dashboard เท่านั้น**
- **🔴 คีย์ production หลุดในแชต (ผู้ใช้วางค่า Railway env ลงแชต) → ต้อง rotate:** `SECRET_KEY`, `R2_ACCESS_KEY_ID`+`R2_SECRET_ACCESS_KEY`, `LINE_CHANNEL_SECRET`, `PROD_ADMIN_PASSWORD` (ค่าเดิมอ่อนมาก). `DATABASE_URL` ปลอดภัย (เป็น reference `${{Postgres_moobaan.DATABASE_URL}}`)
- **OTP เลิกใช้แล้ว (ผู้ใช้แจ้ง 2026-06-12 — มีค่าใช้จ่าย SMS):** login ลูกบ้าน = LINE OAuth อย่างเดียว → ควรปิดให้ขาด `OTP_PROVIDER=disabled` + ลบ `ALLOW_MOCK_OTP_IN_PROD`/`OTP_*`/`SMSMKT_*` บน Railway
  - **แก้ความเข้าใจเดิม:** `ALLOW_MOCK_OTP_IN_PROD=true` **ไม่ใช่** ช่องโหว่เปิดกว้าง — `factory.py` บังคับ production+mock ต้องมี `OTP_SANDBOX_WHITELIST` และเฉพาะเบอร์ใน whitelist เท่านั้นที่ผ่าน; whitelist ว่าง → provider fail-closed (OTP ปิดโดยพฤตินัยอยู่แล้ว). เป็น flag โหมดทดสอบค้าง ไม่ใช่เหตุฉุกเฉิน
  - SMSMKT keys **ไม่ได้หลุด** (ไม่อยู่ในชุดที่วางในแชต) → แค่ลบทิ้งได้ ไม่ต้อง rotate
- **🔴 config เสี่ยงบน Railway (ยังต้องแก้):**
  1. `PROD_RESET_ADMIN_PASSWORD=true` + `RUN_PROD_SEED=true` ค้างไว้ → ทุก deploy reset รหัสแอดมิน/seed ซ้ำ → ต้อง false หลัง setup
  2. **`R2_PUBLIC_URL` (โค้ดอ่าน) ≠ `R2_PUBLIC_BASE_URL` (Railway ตั้ง)** → public URL ไม่ถูกอ่าน (สลิปยังเข้าได้เพราะใช้ backend proxy); `R2_ACCOUNT_ID`/`R2_REGION` โค้ดไม่ใช้
- **ทำแล้ว:** เขียน `RUNBOOK-rotate-secrets.md` (checklist) + อัปเดต `backend/.env.example` ให้ครบทุกคีย์ (ชื่อล้วน ไม่มีค่า)
- **บทเรียน (ย้ำ):** export secret อย่าวางในแชต — เซฟลงไฟล์ตรง ๆ ใน `OneDrive\03-ProjectSecrets`; แอป active = canonical secret อยู่บน dashboard, ต้อง export มาเก็บเข้ารหัสถึงจะนับว่า backup ครบ

### จัด context มาตรฐานให้ตรงเล่มกลาง UnionMyAi
- เพิ่มไฟล์ context มาตรฐานที่ราก repo: `STATUS.md`, `KNOWLEDGE-LOG.md`, `DESIGN.md` + หัวข้อ "🤝 AI Collaboration & Context System" ใน `CLAUDE.md` (กำหนด Claude Code = เจ้าภาพหลัก, read order, กฎเหล็กห้ามทำ prod พัง)
- **เหตุผล:** ให้ AI ตัวอื่นเข้ามาทำงานต่อบนบริบทเดียวกันได้ ตาม `WEB-PROJECT-WORKFLOW.md` ข้อ 1-2 (เหมือนที่ทำกับ sss-corp-erp)
- **วิธีนำไปใช้:** เปิดโปรเจกต์ → อ่าน 4 ไฟล์ตามลำดับ CLAUDE → STATUS → KNOWLEDGE-LOG → DESIGN; งานที่ยังไม่ push/แก้ context บันทึกใน repo นี้ (ไม่ใช่เล่มกลาง)
- **หมายเหตุ:** `AGENTS.md` เป็นสำเนา CLAUDE.md (untracked) → เสี่ยง drift ถ้าแก้ตัวเดียว ควร sync หรือยุบรวม

### บทเรียนจาก payins (สรุปจาก commit ล่าสุด)
- **ค่า amount ที่เป็น NaN/Inf** ทำให้ endpoint list/get และ submit พังได้ → ต้อง guard ทั้ง 2 ฝั่ง (defense in depth: `3ddb825` + `49afdaa`)
- **ปุ่มลบ invoice** เคยเพิ่ม (`659e583`) แล้ว revert (`b89d940`) เพราะเสี่ยงข้อมูลหาย → **อย่าเพิ่มปุ่มลบ invoice กลับโดยไม่มี safeguard**
- **สลิป:** `slip_url` ใน DB เป็น R2 object key ไม่ใช่ URL ตรง → ต้องใช้ proxy `payinsAPI.slipUrl(id)` เสมอ (`2d1a51f` แก้ viewer ให้ใช้ endpoint ที่ auth)
